// @ts-check
import { defineConfig } from 'astro/config';

// Astro configuration.
// Docs: https://astro.build/config
// Kept intentionally minimal for the skeleton — integrations (Tailwind,
// sitemap, etc.) will be added in later phases as the site is built out.
export default defineConfig({
  // TODO: set `site` to the production domain once it is registered
  // (see BRIEF.md — domain not yet owned). Example:
  // site: 'https://linknowevents.com',
});
